import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Register",
  description: "Create a new Emetals account",
};

export default function RegisterPage() {
  return <h1>Register</h1>;
}
